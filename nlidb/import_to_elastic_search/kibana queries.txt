
# Get a list of all indices in the elastic-search server

GET _cat/indices

#GET entire db

GET _search
{
  "query": {
    "match_all": {}
  }
}

#get particular document with matching query_string in any column in all indices
GET _search
{
  "query": {
    "query_string": {
      "query": "Fresno"
    }
  }
}

# Get particular document with matching query_string in any column of a particular index
GET metadata2/_search
{
  "query": {
    "query_string": {
      "query": "Fresno"
    }
  }
}

# Get particular document with matching query_string in a specified column for a particular index
GET final_data/_search
{
  "query": {
    "match": {
      "column_name": "number"
    }
  }
}


#delete everything with index /testpy
DELETE /testpy

# When we know values of multiple fields
GET /_search
{
  "query": {
    "bool": {
      "should": [
        { "match": { "title":  "War and Peace" }},
        { "match": { "author": "Leo Tolstoy"   }}
      ]
    }
  }
}